using UnityEngine;

public class MainCameraManager : MonoBehaviour
{
    //This script is responsible for global camera's and active camera controllers
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
